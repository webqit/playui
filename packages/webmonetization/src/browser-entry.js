
/**
 * @imports
 */
import WebMonetization from './index.js';

// As globals
if (!window.OnePhrase) {
	window.OnePhrase = {};
}
if (!window.OP) {
	window.OP = {};
}
window.OnePhrase.WebMonetization = WebMonetization;
window.OP.WebMonetization = WebMonetization;
